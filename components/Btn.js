import React from "react";
import styled from "styled-components";

function btn() {
 return (
    <Btn>
        <button>게시물 추가</button>
    </Btn>
 )
};

const Btn = styled.div`
 color: yellow;
`;

export default Btn;